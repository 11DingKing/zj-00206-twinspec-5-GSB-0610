services:
  twinspec:
    build: .
    container_name: twinspec-api
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./dev.db
      - NODE_ENV=production
    volumes:
      - ./prisma/dev.db:/app/prisma/dev.db
    restart: unless-stopped
